# Django
DJANGO_SECRET_KEY=local-development-only-change-me
DJANGO_DEBUG=true
DJANGO_ALLOWED_HOSTS=127.0.0.1,localhost

# PostgreSQL
POSTGRES_DB=aftermuse
POSTGRES_USER=aftermuse
POSTGRES_PASSWORD=aftermuse-local
POSTGRES_HOST=127.0.0.1
POSTGRES_PORT=5432

# External metadata providers
# Leave empty for tests and development without live Aladin requests.
# A valid, approved key is required only for live Aladin searches.
ALADIN_TTB_KEY=
